<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <carbon.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        tools:context=".main.MainActivity">

        <carbon.widget.FrameLayout
            android:id="@+id/fl_bottomNavigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:carbon_elevation="@dimen/_9sdp"
            app:carbon_elevationShadowColor="@color/color_black"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="1.0">

            <com.google.android.material.bottomnavigation.BottomNavigationView
                android:id="@+id/bottomNavigation"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/colorWhite"
                app:elevation="@dimen/_36sdp"
                android:theme="@style/Widget.BottomNavigationView"
                app:itemIconTint="@color/bnv_tab_item_foreground"
                app:itemTextAppearanceActive="@style/Widget.BottomNavigationView.itemActive"
                app:itemTextAppearanceInactive="@style/Widget.BottomNavigationView.itemDeActive"
                app:itemTextColor="@color/bnv_tab_item_foreground"
                app:labelVisibilityMode="labeled"

                app:menu="@menu/navigation_menu" />

        </carbon.widget.FrameLayout>

        <fragment
            android:id="@+id/navHostFragment"
            android:name="androidx.navigation.fragment.NavHostFragment"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_above="@id/fl_bottomNavigation"
            app:defaultNavHost="true"
            app:layout_constraintBottom_toTopOf="@+id/fl_bottomNavigation"
            app:layout_constraintTop_toTopOf="parent"
            app:navGraph="@navigation/main_nav_graph" />


    </carbon.widget.ConstraintLayout>

</layout>